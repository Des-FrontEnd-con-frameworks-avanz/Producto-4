import { FiltroEdadPipe } from './filtro-edad.pipe';

describe('FiltroEdadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEdadPipe();
    expect(pipe).toBeTruthy();
  });
});
